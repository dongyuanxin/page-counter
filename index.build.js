import PageCounter from './src'

PageCounter.setData()
PageCounter.countTotal()
PageCounter.countSingle()

/**
 * Complie Bomb sdk and add Bomb to window:
 * ```javascript
 *    import Bomb from 'hydrogen-js-sdk'
 *    window.Bomb = Bomb
 * ```
 */